class_name SkillStats
extends Resource

@export var name: String
@export var texture: Texture2D
@export var duration: int = 120 # in frames
@export var radius: int = 200 # in pixels


func effect(area: Node) -> void:
	return

